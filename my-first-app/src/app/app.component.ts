import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  // can include inline template by templaete: '<app-component></app-component>
  styleUrls: ['./app.component.css']
  // styleUrls is an [], so multiple links possible
  // also, inline styling is also allowed
})
export class AppComponent {
  // title = 'Angular App'
  // name = 'Adwait'
}
